[build]
  command = "yarn build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Suporte a client-side routing (react-router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
